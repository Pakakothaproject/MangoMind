// This file is kept to prevent breaking imports in older components that haven't been migrated to use useModelStore yet.
// The source of truth for models is now the database via `useModelStore`.
export const ALL_CHAT_MODELS = [];
